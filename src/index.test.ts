import Subject from "./";

describe(Subject.name, () => {
    test.todo("preserves modules with no replacement");
    test.todo("supports replacement of file with no extension");
    test.todo("supports replacement of file with qualified with an extension");
    test.todo("supports replacement of file with resolvable extension");
    test.todo("supports replacement of main file with resolvable extension");
    test.todo("does not replace main files without a resolvable extension");
});
